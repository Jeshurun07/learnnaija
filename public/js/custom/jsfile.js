/*js files*/

//alert("Welcome to Learnnaija");
$(function() {
    var selectedVal = $("level").value;
    if (($level).selected) {
        alert($level.value);
    }
    $("level").("select", display)
});